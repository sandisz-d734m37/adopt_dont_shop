<%= "Add a Pet to this Application" %>
<%= form_with url: "/applications/#{@application.id}?search=#{params[:pet_name]}", method: :get, local: true do |f| %>
<%= f.label :query, "Pet name" %>
<%= f.text_field :query, name: :pet_name %>
<%= f.submit "Search" %><br>
<% end %>
